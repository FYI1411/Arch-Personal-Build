#! /bin/bash
# This script must be sourced from within a shell
# and not executed. For instance with:
# 
# . path/to/that/script

if [ "$1" == '-m' ]; then
	python -m venv "$2"
else
	. "$1/bin/activate"
fi
